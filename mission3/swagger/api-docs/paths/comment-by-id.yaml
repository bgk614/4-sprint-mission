patch:
  tags:
    - comment
  summary: 댓글 수정
  operationId: updateComment
  parameters:
    - $ref: '#/components/parameters/commentId'
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/UpadateComment'
  responses:
    '200':
      description: (OK) 댓글 수정 성공
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Comment'

delete:
  tags:
    - comment
  summary: 댓글 삭제
  operationId: deleteComment
  parameters:
    - $ref: '#/components/parameters/commentId'
  responses:
    '204':
      description: (No Content) 댓글 삭제 성공
